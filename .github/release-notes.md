Verona adds JSON-friendly support for `.vomd`, `.vocs`, and `.voud` files in both VS Code and JetBrains IDEs.

Included assets:

- VS Code extension package (`.vsix`)
- JetBrains plugin package (`.jar`)
- JetBrains plugin archive (`.zip`)
